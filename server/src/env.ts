const { env } = process;

export const { NODE_ENV } = env;

export const { PORT } = env;

export const { MONGODB_URL } = env;

export const { COOKIE_SECRET } = env;

export const { CLIENT_URL } = env;
